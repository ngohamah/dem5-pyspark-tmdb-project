.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.DS_Store
IMPLEMENTATION_MAP.md

# Local secrets -- copy .env.example to .env and fill in your own password
.env

# Runtime data: generated CSVs, Spark checkpoint state, rejected-row dumps.
# Keep the directories (via .gitkeep) but not their contents.
data/incoming/*.csv
data/rejected/*.csv
checkpoints/*
!checkpoints/.gitkeep
